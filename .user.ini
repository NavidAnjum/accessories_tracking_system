; Make PHP re-check files on every request so uploads take effect immediately.
; This removes the need to manually clear OPcache after each deploy.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
